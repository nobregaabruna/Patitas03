import { PostDiv } from "./style";

type PostInfo = {
    fotos: Array<string>;
    userName: string;
    userPic: string;
    local: string;
    time: Date;
    descricao: string;
}

export default function Post() {
    return (
        <PostDiv>

        </PostDiv>
    );
}